import 'package:flutter/material.dart';

class ChatMedView extends StatelessWidget {
  const ChatMedView({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}

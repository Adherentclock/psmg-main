import 'package:flutter/material.dart';

class EscanerScreen extends StatelessWidget {
  const EscanerScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('APP Escaner'));
  }
}

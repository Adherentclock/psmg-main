import 'package:flutter/material.dart';

class PrediagnosticoScreen extends StatelessWidget {
  const PrediagnosticoScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Prediagnostico'));
  }
}
